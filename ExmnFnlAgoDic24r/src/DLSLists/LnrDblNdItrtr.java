package DLSLists;import ADTsException.*;import java.util.Iterator;public class LnrDblNdItrtr<T> implements Iterator<T> {   private int total;  // the number of elements in the structure   private LnrDblNd<T> currentNode;  // the currentNode position   private int ctrlvar;   public LnrDblNdItrtr (LnrDblNd<T> initnd, int nitems) {      currentNode = initnd;      total = nitems;      ctrlvar = 0;   }   public boolean hasNext() {       boolean b= (ctrlvar < total) && (currentNode.getNext()!=null);      return b;   }   public T next() throws ADTsException {      if (! hasNext())         throw new ADTsException("DblyLnkSctrIterator next(): No Such Element");      T result = currentNode.getElement();      currentNode = currentNode.getNext();      ctrlvar++;      return result;   }   public void remove() throws UnsupportedOperationException {      throw new UnsupportedOperationException();   }}